version: '3'

services:
  nginx-web:
    image: codertravel/nginx-web:latest
    ports:
      - "8080:80"

  haproxy-lb:
    build:
      context: ./haproxy-lb
      dockerfile: Dockerfile
    image: haproxy-lb
    ports:
      - "80:80"

version: '3'

services:
  nginx-web:
    image: codertravel/nginx-web:latest
    ports:
      - "80:80"

  haproxy-lb:
    build:
      context: ./haproxy-lb
      dockerfile: Dockerfile
    image: haproxy-lb
    ports:
      - "8080:8080"
